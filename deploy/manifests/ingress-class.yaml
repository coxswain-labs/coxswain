apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: coxswain
  labels:
    app.kubernetes.io/name: coxswain
spec:
  # Must match COXSWAIN_CONTROLLER_NAME in the deployment environment.
  controller: coxswain-labs.dev/gateway-controller
  # Uncomment to enable per-class annotation defaults via CoxswainIngressClassParameters.
  # Requires deploy/manifests/crds/coxswainingressclassparameters.yaml to be installed first.
  # See deploy/dev/sample-ingress-class-parameters.yaml for a sample CR.
  #
  # parameters:
  #   apiGroup: ingress.coxswain-labs.dev
  #   kind: CoxswainIngressClassParameters
  #   name: coxswain-defaults
  #   namespace: coxswain-system
  #   scope: Namespace
