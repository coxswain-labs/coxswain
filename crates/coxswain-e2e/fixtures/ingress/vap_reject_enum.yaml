# VAP rejection fixture: invalid enum annotation value.
#
# `backend-protocol: "websocket"` is not a valid enum member (valid values:
# http, https, h2c, grpc, grpcs). The VAP must reject this Ingress at admission.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vap-reject-enum
  annotations:
    ingress.coxswain-labs.dev/backend-protocol: "websocket"
spec:
  ingressClassName: coxswain
  rules:
    - host: vap-enum.${TESTNS}.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: echo-a
                port:
                  number: 3000
