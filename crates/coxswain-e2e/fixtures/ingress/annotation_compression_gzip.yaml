apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: compression-gzip-ingress
  annotations:
    ingress.coxswain-labs.dev/compression-gzip: "true"
    ingress.coxswain-labs.dev/compression-level: "6"
    ingress.coxswain-labs.dev/compression-types: "application/json,text/html,text/plain"
    ingress.coxswain-labs.dev/compression-min-size: "1"
spec:
  ingressClassName: coxswain
  rules:
    - host: compression-gzip.${TESTNS}.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: echo-a
                port:
                  number: 3000
