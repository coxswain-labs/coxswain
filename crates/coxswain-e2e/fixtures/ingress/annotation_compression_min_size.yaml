apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: compression-min-size-ingress
  annotations:
    ingress.coxswain-labs.dev/compression-gzip: "true"
    ingress.coxswain-labs.dev/compression-min-size: "1048576"
spec:
  ingressClassName: coxswain
  rules:
    - host: compression-minsize.${TESTNS}.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: echo-a
                port:
                  number: 3000
