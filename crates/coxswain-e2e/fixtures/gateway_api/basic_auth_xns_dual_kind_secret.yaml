# Cross-namespace BasicAuth, dual-kind-grant variant: Secret-only tenant side.
#
# Just the htpasswd Secret, with NO ReferenceGrant — the grant lives in the
# companion basic_auth_xns_dual_kind_grant.yaml so a test can assert the
# fail-closed state before granting, then grant and assert the flip.
#
# Credentials: alice:secret (bcrypt).
---
apiVersion: v1
kind: Secret
metadata:
  name: gw-auth-htpasswd
  labels:
    ingress.coxswain-labs.dev/auth-basic: "true"
type: Opaque
data:
  auth: YWxpY2U6JDJ5JDA0JHdyUkZRU0NCZXpZTFR5V1hKS1dldXVPaHRGdWtyQWo3UHpQWXRRc09ORWg4ck9PampJTGFLCg==
