{{- if .Values.proxy.shared.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "coxswain.sharedProxy.fullname" . }}-internal
  namespace: {{ include "coxswain.namespace" . }}
  labels:
    {{- include "coxswain.sharedProxy.selectorLabels" . | nindent 4 }}
  {{- with .Values.service.internal.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  selector:
    {{- include "coxswain.sharedProxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: health
      port: {{ .Values.health.port }}
      targetPort: health
      protocol: TCP
    - name: admin
      port: {{ .Values.admin.port }}
      targetPort: admin
      protocol: TCP
{{- end }}
